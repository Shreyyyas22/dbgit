# dbgit

Git for Databases — version control for your data.

## Features

- Version control wrap over any PostgreSQL database
- Git semantics (commit, branch, merge, diff, rollback)
- Pull requests for data changes

## Quickstart

```bash
docker compose up -d
alembic upgrade head
```
